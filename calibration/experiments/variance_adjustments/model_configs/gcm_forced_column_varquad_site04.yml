# HadGEM2-A AMIP July; cfSite 4. Merge after `gcm_forced_column_varquad_hires.yml` (shared mesh/time/forcing).
# `cfsite_number` defaults to site4 in hires — this file only fixes the forward-sweep output folder slug.
forward_sweep_case_slug: GCM_CFSITE04
