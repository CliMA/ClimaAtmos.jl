# GoogleLES coupling (stub)

No implementation yet. Future work: ingest GoogleLES / MLCondensateDistributions-style columns as
reference `y` or forcing, reusing the observation pipeline in `observation_map.jl`.
