prior: prior.toml
ensemble_size: 100
n_iterations: 10
model_config: model_config.yml
observations: true_observations.jld2
noise: narrow_obs_noise.jld2
output_dir: output/narrow_uncertainty
batch_size: 1
y_var_names: [clwvi, clvi, prw, hurvi, rlut, rlutcs, rsut, rsutcs]
dims: 8 # num vertical levels x num variables, also shape of observations
g_t_start_sec : 144000.0 # 40 hrs
g_t_end_sec : 288000.0 # 80 hrs
