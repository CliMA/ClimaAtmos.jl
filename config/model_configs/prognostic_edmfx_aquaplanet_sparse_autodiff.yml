# This job takes too long so we use a lower top and coarser vertical resolution
z_max: 30000.0
z_elem: 10
dz_bottom: 500.0
rayleigh_sponge: false
viscous_sponge: false
dt: 20secs
surface_setup: DefaultMoninObukhov
rad: clearsky
turbconv: prognostic_edmfx
prognostic_tke: true
edmfx_entr_model: "Generalized"
edmfx_detr_model: "Generalized"
edmfx_nh_pressure: true
edmfx_vertical_diffusion: true
edmfx_filter: true
edmfx_sgs_mass_flux: true
edmfx_sgs_diffusive_flux: true
implicit_diffusion: true
use_auto_jacobian: true
# Run the measured padding mode. runtime_remeasure starts from the
# transient-safety constant mask, then switches to the lean measured mask of
# the developed state; debug_jacobian prints the Layer 1 dense-vs-constant and
# dense-vs-measured comparison at the final state.
auto_jacobian_padding_mode: measured
auto_jacobian_runtime_remeasure: true
update_jacobian_every: stage
ode_algo: "ARS222"
debug_jacobian: true
microphysics_model: "0M"
t_end: 10mins
dt_save_state_to_disk: 600secs
toml: [toml/prognostic_edmfx.toml]
output_default_diagnostics: false
diagnostics:
  - short_name: [ts, ta, thetaa, ha, pfull, rhoa, ua, va, wa, hfss, hfls, hur, hus, cl, clw, cli, evspsbl, pr, rsd, rsu, rld, rlu]
    reduction_time: average
    period: 10mins
  - short_name: [arup, waup, taup, thetaaup, haup, husup, hurup, clwup, cliup, waen, tke, lmix]
    reduction_time: average
    period: 10mins
